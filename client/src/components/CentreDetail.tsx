import * as React from 'react';

/**
 * Detail plan / blueprint of given centre
 * @param props
 * @returns {any}
 * @constructor
 */
export const CentreDetail = (props: any) => {
    return (<div/>);
};
